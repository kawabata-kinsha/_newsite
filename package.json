{
  "name": "newsite",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:sass": "node-sass src/scss/ --output www/assets/css/ --output-style compressed --watch --source-map true",
    "sass:master": "node-sass src/scss/ --output www/assets/css/ --output-style compressed",
    "watch:es": "webpack --watch",
    "es:master": "webpack --mode production",
    "clean:css": "rimraf www/assets/css && mkdir -p www/assets/css",
    "clean:js": "rimraf www/assets/js && mkdir -p www/assets/js",
    "watch": "npm-run-all -p watch:*",
    "build:master": "npm run clean:css && npm run sass:master && npm run clean:js && npm run es:master"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.10.1",
    "@babel/preset-env": "^7.10.1",
    "babel-loader": "^8.1.0",
    "node-sass": "^4.14.1",
    "npm-run-all": "^4.1.5",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  }
}
